// These modules bind to app 'riot', so every controller, service, directive will be bound to app if it is bound to one of these
angular.module('riot.directives', []);
angular.module('riot.controller', []);
angular.module('riot.controller.ui', []);
angular.module('riot.services', []);
